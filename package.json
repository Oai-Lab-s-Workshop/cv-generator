{
  "scripts": {
    "test": "bun run --cwd frontend test",
    "test:unit": "bun run --cwd frontend test:unit",
    "test:integration": "bun run --cwd frontend test:integration",
    "test:desktop": "bun run --cwd desktop test",
    "desktop:prepare": "bun run --cwd desktop prepare:desktop",
    "desktop:build": "bun run --cwd desktop build",
    "desktop:build:stable": "bun run --cwd desktop build:stable",
    "desktop:dev": "bun run --cwd desktop dev"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0"
  }
}
